---
interface Props {
  id?: string;
}

const { id } = Astro.props;
---

<!-- The route owns the document-level h1. MDX-authored `#` headings belong
     beneath the canonical article title. -->
<h2 id={id}><slot /></h2>
